# ── Thredion Engine — Environment Variables ──────────────────
# Copy this file to .env and fill in your values.

# ── Database Configuration ────────────────────────────────────
# Option 1: Supabase PostgreSQL (RECOMMENDED for production)
#   Get SUPABASE_DB_PASSWORD from: Dashboard > Project Settings > Database > Reveal password
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_DB_PASSWORD=your-real-postgres-password-here

# Option 2: Direct PostgreSQL URL (if not using Supabase)
# Format: postgresql://username:password@host:port/database
# DATABASE_URL=postgresql://user:password@localhost:5432/thredion

# Option 3: SQLite (local development only - always works)
DATABASE_URL=sqlite:///./thredion.db

# Environment: production or development (affects error handling)
ENVIRONMENT=production

# ── OpenAI API Key ────────────────────────────────────────────
# For AI classification & summarization
# Leave blank to use keyword-based fallback (slower but free)
OPENAI_API_KEY=sk-...

# ── Groq Cloud LLM (FREE TIER) ────────────────────────────────
# Free alternative to OpenAI for fast inference
GROQ_API_KEY=

# ── Twilio WhatsApp Integration ───────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890

# ── Render Deployment (Environment Variables) ────────────────
# These are auto-set by Render, but you can override:
# HOST=0.0.0.0
# PORT=8000

# ── Frontend URL (for CORS) ───────────────────────────────────
FRONTEND_URL=https://thredion-v2.vercel.app

# ── JWT Authentication ───────────────────────────────────────
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRY_HOURS=168

# ── Embedding Configuration ───────────────────────────────────
# Options: "sentence-transformers" (local) or "openai" (API-based)
EMBEDDING_MODEL=sentence-transformers

# ── Cognitive Thresholds ──────────────────────────────────────
SIMILARITY_THRESHOLD=0.55
RESURFACING_THRESHOLD=0.60
MAX_CONNECTIONS=5

# ── Video Processing ─────────────────────────────────────────
SHORT_VIDEO_MAX_DURATION=300
MEDIUM_VIDEO_MAX_DURATION=1800
WHISPER_MODEL_SIZE=base

